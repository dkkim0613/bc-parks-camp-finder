name: Daily BC Parks dashboard refresh

on:
  schedule:
    # Hourly through the midnight Vancouver window (covers both PDT and PST,
    # 07:00-10:00 UTC), since GitHub can delay scheduled runs by several
    # hours. The refresh script itself only does work once per Vancouver
    # calendar day, so the extra hourly triggers are cheap no-ops once the
    # day's refresh has landed.
    - cron: "0 7-10 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: Run refresh even if today's Vancouver refresh already happened
        required: false
        default: true
        type: boolean
  repository_dispatch:
    types: [manual-refresh]

permissions:
  contents: write

concurrency:
  group: bc-parks-daily-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: TEMP recon of BC Parks reservation site
        run: |
          echo "== bcparks.ca headers =="
          curl -sS -D - -o /tmp/home.html "https://bcparks.ca/" | head -20
          echo "== camping.bcparks.ca headers =="
          curl -sS -D - -o /tmp/camping.html "https://camping.bcparks.ca/" | head -20
          echo "== camping.html size =="
          wc -c /tmp/camping.html
          echo "== script/link asset tags =="
          grep -oE '(src|href)="[^"]+"' /tmp/camping.html | sort -u | head -60
          echo "== possible api/domain hints in camping.html =="
          grep -oiE 'https?://[a-z0-9.-]+' /tmp/camping.html | sort -u | head -40

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Refresh dashboard metadata
        env:
          FORCE_REFRESH: ${{ github.event_name != 'schedule' && 'true' || 'false' }}
        run: node scripts/refresh-github-data.mjs

      - name: Build
        run: pnpm build

      - name: Commit refresh result
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Refresh BC Parks dashboard metadata"
          file_pattern: public/refresh-status.json
